<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyForm"
        xmlns:Model="clr-namespace:MyForm.Model" xmlns:ViewModel="clr-namespace:MyForm.ViewModel" x:Class="MyForm.View.MainWindow"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="405">
    <Window.DataContext>
        <ViewModel:MyViewModel/>
    </Window.DataContext>
    <Grid>
        <TabControl>
            <TabItem Header="{Binding Title, Mode=OneWay}">
                <ListView>

                </ListView>
                <!--<StackPanel>
                    <Grid Margin="5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="2*"/>
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0" Margin="10,0,0,0"/>
                        <TextBox Grid.Column="1" Margin="0,0,50,0"/>
                    </Grid>
                </StackPanel>-->
        
            </TabItem>
            <TabItem Header="Tabs"/>
            <TabItem Header="Obfuscation"/>
        </TabControl>
    </Grid>

</Window>

